export function Footer() {
  return (
    <footer className="border-t border-border/60 bg-card">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-6 py-8 text-sm text-muted-foreground md:flex-row md:items-center md:justify-between">
        <p>VendorsHub helps Ghanaian vendors run operations, storefronts, and reporting from one place.</p>
        <p>Built for scale-ready commerce teams.</p>
      </div>
    </footer>
  );
}